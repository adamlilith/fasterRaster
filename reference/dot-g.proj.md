# Call GRASS `g.proj` tool

This function calls the **GRASS** tool `g.region` to display information
on the projection of the current **GRASS** "project".

## Usage

``` r
.g.proj()
```

## Value

Displays current projection information for the active
"project/location" in **GRASS**.
