# Names of columns of a GRASS vector's attribute table

This function returns the column names of a **GRASS** vector's attribute
table.

## Usage

``` r
.vNames(x)
```

## Arguments

- x:

  A `GVector` or the name of a vector in **GRASS**.

## Value

Character vector.
