# Call GRASS `g.region` tool

This function calls the **GRASS** tool `g.region` to display information
on the region of the current **GRASS** "project".

## Usage

``` r
.g.region()
```

## Value

Displays current region information for the active "project/location" in
**GRASS**.
